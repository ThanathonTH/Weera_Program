# ------------------------------------------------
# 🐍 Python Standard
# ------------------------------------------------
__pycache__/
*.py[cod]
*$py.class

# ------------------------------------------------
# 🛠️ Environments & Secrets (สำคัญมาก ห้ามหลุด)
# ------------------------------------------------
.env
.venv
env/
venv/
ENV/

# ------------------------------------------------
# 🏗️ Build & Distribution (ไฟล์ที่สร้างใหม่ได้ตลอด)
# ------------------------------------------------
build/
dist/
*.spec
*.exe
*.zip
*.installer

# ------------------------------------------------
# ⚙️ Local Configuration (ค่าส่วนตัวแต่ละเครื่อง)
# ------------------------------------------------
settings.json
# (ถ้ามีไฟล์ log ก็ควรใส่ด้วย)
*.log

# ------------------------------------------------
# 📦 Heavy Dependencies / Assets (ให้ Updater จัดการ)
# ------------------------------------------------
engine/
font/
# หมายเหตุ: icon/ ควรเอาขึ้น Git เพราะขนาดเล็กและจำเป็นตอน Build
# แต่ถ้าคุณไม่อยากเอาขึ้นจริงๆ ก็เพิ่มบรรทัด icon/ ลงไปได้ครับ

# ------------------------------------------------
# 💻 IDE & OS Junk Files (ไฟล์ขยะของ Editor)
# ------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db